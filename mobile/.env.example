# Supabase — copy to mobile/.env and fill from Supabase → Settings → API.
# EXPO_PUBLIC_ prefix is required so Expo exposes these to the client bundle.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
